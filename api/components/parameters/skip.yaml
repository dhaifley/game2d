# components/parameters/skip.yaml
name: skip
in: query
schema:
  type: integer
  minimum: 0
  maximum: 10000
  default: 0
description: >
  The number of results that should be skipped before any size limit.
